# Scrapy project config for B2B enrichment
# This must be at the repo root so subprocess scrapy crawl commands
# can find the project when run with cwd=/root/empire-v49.
[settings]
default = scrapy_b2b.settings

[deploy]
project = scrapy_b2b
