/**
 * Empire AI · Contractor-recruit chat widget
 * STUB — full implementation owned by buffy, see /root/CHAT_WIDGET_SPEC.md
 * Date: 2026-06-14
 *
 * This stub does nothing visible. The /contractors page references
 * /static/contractors/chat.js — when the real widget ships, this file
 * gets overwritten. The page handles the missing-widget case gracefully
 * (no chat bubble renders).
 *
 * Real widget requirements (from spec):
 *   - Floating chat bubble, bottom-right
 *   - POST /api/contractors/chat { session_id, message }
 *   - Render reply, rate-limit at 30/hr/session
 *   - Synthetic_brain /ask integration with the contractor-recruit
 *     system prompt
 */
(function () {
  // Intentionally empty until buffy ships the real widget.
  // See /root/CHAT_WIDGET_SPEC.md for the spec.
  console.log("[empire-ai chat] stub loaded; real widget pending buffy");
})();
